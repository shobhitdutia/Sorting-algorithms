The programs
It is highly recommended to increase the heap size of the JVM before executing the code to avoid a out of memory error. For example:
java -Xms1024m -Xmx1024m -Xss64m [filename] [sizeOfInput]

The file should contain only the unsorted numbers
